<%= form_with(model: trucker, local: true) do |form| %>
  <% if trucker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(trucker.errors.count, "error") %> prohibited this trucker from being saved:</h2>

      <ul>
      <% trucker.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :trucker_name %>
  </div>

  <div class="field">
    <%= form.label :phone %>
    <%= form.text_field :phone, id: :trucker_phone %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
